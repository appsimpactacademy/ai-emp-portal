<div class="col-lg-12">
  <p style="color: green"><%= notice %></p>

  <%= render @employee %>

  <div>
    <%= link_to "Edit", edit_admin_employee_path(@employee), class: "btn btn-primary" %>
    <%= link_to "Back", admin_employees_path, class: "btn btn-outline-primary" %>

    <%= link_to "Delete", admin_employee_path(@employee), data: { turbo_method: :delete, turbo_confirm: "Are you sure?" }, class: "btn btn-danger" %>
  </div>
</div>
