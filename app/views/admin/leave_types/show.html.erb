<div class="col-lg-12">
  <p style="color: green"><%= notice %></p>

  <%= render @leave_type %>

  <div>
    <%= link_to "Edit", edit_admin_leave_type_path(@leave_type), class: "btn btn-primary" %>
    <%= link_to "Back", admin_leave_types_path, class: "btn btn-outline-primary" %>

    <%= link_to "Delete", admin_leave_type_path(@leave_type), data: { turbo_method: :delete, turbo_confirm: "Are you sure?" }, class: "btn btn-danger" %>
  </div>
</div>
